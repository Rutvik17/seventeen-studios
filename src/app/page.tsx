import { Film } from '@/components/film/Film';

/**
 * The landing: one scene, filling the screen — Nvidia's campus in Santa
 * Clara, drawn, painted and turned through a year. The rest of the book is
 * one tab away in the header.
 */
export default function HomePage() {
  return <Film />;
}
