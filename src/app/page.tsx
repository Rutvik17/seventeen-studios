import { Contents } from '@/components/sections/Contents';

/**
 * The landing: the first page of the sketchbook — its title and its contents.
 * Every chapter it lists is a page-turn away. The book's back endpaper, with
 * the way to reach its owner, is the footer every page shares.
 */
export default function HomePage() {
  return <Contents />;
}
